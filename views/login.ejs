<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Login</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/css/bootstrap.min.css"/>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <style>
        body {
            background-color: #f7f7f7;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            font-family: 'Roboto', sans-serif;
            margin: 0;
        }
        .container {
            background-color: #fff;
            border-radius: 15px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
            display: flex;
            width: 90%;
            max-width: 800px;
            overflow: hidden;
        }
        .login-form {
            padding: 40px;
            flex: 1;
        }
        .panel-title {
            font-size: 2rem;
            font-weight: bold;
            color: #28a745;
            margin-bottom: 30px;
        }
        .form-control {
            border-radius: 0.5rem;
        }
        .btn-login {
            background-color: #28a745;
            color: #fff;
            border-radius: 0.5rem;
            font-size: 1.2rem;
        }
        .btn-login:hover {
            background-color: #218838;
        }
        .control a {
            color: #28a745;
            text-decoration: none;
        }
        .control a:hover {
            text-decoration: underline;
        }
        .login-image {
            width: 50%; /* Adjusted to take half of the container */
            overflow: hidden; /* Ensures no overflow */
        }
        .login-image img {
            width: 100%; /* Makes the image responsive */
            height: auto; /* Maintains aspect ratio */
            object-fit: cover; /* Ensures the image covers the area */
            border-radius: 0 15px 15px 0;
        }
        .form-group {
            margin-bottom: 1.5rem;
        }
        .logo {
            display: block;
            margin: 0 auto 20px; /* Centers the logo */
            max-width: 80px;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="login-form">
        <div class="text-center mb-4">
            <img src="/img/logo.jpeg" alt="Library Logo" class="logo img-fluid">
        </div>
        <div class="panel-title text-center">Library Login</div>
        <form id="loginform" class="form-horizontal" role="form" method="post" action="/login">
            <div class="form-group">
                <label for="login-username" class="form-label">Username</label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-person"></i></span>
                    <input id="login-username" type="text" class="form-control" name="user_name" placeholder="Username" required>
                </div>
            </div>
            <div class="form-group">
                <label for="login-password" class="form-label">Password</label>
                <div class="input-group">
                    <span class="input-group-text"><i class="bi bi-lock"></i></span>
                    <input id="login-password" type="password" class="form-control" name="password" placeholder="Password" required>
                </div>
            </div>
            <div class="form-group">
                <div class="d-grid">
                    <button id="btn-login" type="submit" class="btn btn-login btn-lg">Login</button>
                </div>
            </div>
            <div class="form-group text-center mt-4">
                <div class="control">
                    <div style="font-size: 85%">
                        Don't have an account?
                        <a href="/signup">Sign Up Here</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
    <div class="login-image">
        <img src="/img/library_image.jpeg" alt="Library Image" class="img-fluid">
    </div>
</div>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/5.1.3/js/bootstrap.bundle.min.js"></script>
</body>
</html>
